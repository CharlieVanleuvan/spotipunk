steps:
  # Step 1: Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/nifty-catfish-458019-a0/spotipunk-image/spotipunk-image:$COMMIT_SHA', '.']

  # Step 2: Push the image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'us-central1-docker.pkg.dev/nifty-catfish-458019-a0/spotipunk-image/spotipunk-image:$COMMIT_SHA']

  # Step 3: Update the Cloud Run Job to use the new image
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'jobs'
      - 'update'
      - 'spotipunk-pipeline'
      - '--image'
      - 'us-central1-docker.pkg.dev/nifty-catfish-458019-a0/spotipunk-image/spotipunk-image:$COMMIT_SHA'
      - '--region'
      - 'us-central1'

images:
  - 'us-central1-docker.pkg.dev/nifty-catfish-458019-a0/spotipunk-image/spotipunk-image:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
